interface:
  display_name: "Finanzas Sommos — Runway y Dashboard"
  short_description: "Analiza caja, core burn, runway, escenarios, KPIs y estado de cierre ejecutivo de Sommos."
  brand_color: "#4D0B95"
  default_prompt: "Usa $finanzas-runway-dashboard para revisar o actualizar Runway Mensual y Dashboard, validando caja, core burn, escenarios, próximos 30 días, KPIs y checks del modelo sin reconstruir lógica contable que ya existe aguas arriba."

policy:
  allow_implicit_invocation: true
